import React from 'react'

const MCPPage = () => {
  return (
    <div
      className={`flex h-screen min-h-[600px] w-full relative p-[10px] duration-200 ease-[cubic-bezier(0.65,0,0.35,0)]`}
    >
      mcp
    </div>
  )
}

export default MCPPage
