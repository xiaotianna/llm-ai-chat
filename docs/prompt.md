const promptVar = '方太是一家中国知名的厨房电器品牌，专注于高端厨电领域，致力于为消费者提供高品质、创新性的厨房解决方案'
    const res = await fetchGpt(
      // '要求只生成主题关键词，每个关键词字数控制在 3-4个字以内，生成数量必须在10-15个，关键词必须以逗号隔开，主题跟企业属性 短视频 拍摄相关，例如：亲子教育,专业指导,育儿经验'
      // '要求只生成主题关键词，每个关键词字数控制在 3-4个字以内，生成数量必须在10-15个，关键词必须以逗号隔开，主题跟企业属性 短视频 拍摄相关，例如：企业属性是：方太是一家中国知名的厨房电器品牌，专注于高端厨电领域，致力于为消费者提供高品质、创新性的厨房解决方案 生成的关键词是：智能厨电,品质生活,厨电标杆'
      `你的任务是根据给定的企业属性，生成与企业属性和短视频拍摄相关的主题关键词。
以下是企业属性：
<企业属性>
  {{COMPANY_ATTRIBUTE}}
</企业属性>
{{COMPANY_ATTRIBUTE}}变量内容为：${promptVar}
请按照以下要求生成关键词：
1. 每个关键词的字数控制在3 - 4个字以内。
2. 生成的关键词数量必须在10 - 15个。
3. 关键词之间用','隔开，不要额外内容，便于我做切割。
`